# 项目当前状态与接手指南

更新：2026-09-08。当前源码 **1.4.0**，数据库 **schema 5**。用户反馈旧版已在机房连接正常；新版功能仍待现场复测。仓库 chuiyukong/classroom-assistant 必须保持私有。

## 1.4.0 — 2026-09-08

- 签到取消前端迟到宽限；截止时刻起仍接受签到并计为迟到，倒计时改为已超时时长。教师手动结束签到/课堂后才拒绝学生提交，未签到转缺勤。旧已关闭课堂不自动重开。
- 教师/学生使用一致的深绿开放、橙色迟到签到、灰色关闭状态块；收紧 64 座图和组间距，点名在 1366×768 下展示完整座位与讲台；未签到座位增加浅红提示。
- “开始新的一节课”改为“开始本节课堂”，明确考勤/点名共用一次课堂快照，不重复开始。
- 新建班级独立选择年份、上/下学期；同名班可按学期区分，数据管理增加筛选。其他课堂页仅显示班级名；旧班保留并标为未设置学期。
- schema 5 重建班级唯一约束为名称+年份+学期，保留编号及全部关联；自动备份，不修改已发布迁移。
- 新增完整功能流程图、全部功能说明、核心与完整人工验收指南；README 收录总览，每次发布同步维护。
- 验证：58 项 pytest、原选座浏览器检查、真实一分钟签到跨截止及手动关闭检查通过；实际 EXE 检查见验收记录。Win7/极域与 Excel/WPS 新模板打印仍待现场复测。

## 1.3.0 历史决定（时限规则已由 1.4.0 覆盖）

- 学生默认考勤页；教师开放登记时才出现选座入口。学生点击真实座位图并输入姓名签到，不使用自动刷新的下拉框。
- 考勤、点名共用 seat-map.js，布局来自服务端；输入框不随轮询重建。点名只保留本节课堂开始和点名，不包含签到操作。
- 截止时间由服务端保存，默认 10 分钟，可设 1—40；1.4.0 起到时仍可迟到签到，教师结束签到才将未签到计为缺勤。旧版已开放且无截止时间的课堂保留教师控制，不追设时间。
- 原装配置识别改为 JSON 内容比较 + Excel 文件校验，修复 Windows CRLF 未升级；自定义文件继续保留。
- 长期换座申请只在本节结束前审批；批准通过 seating.approve_move 更新固定座位，目标已有登记或申请来源座位已变更时拒绝。学生的设备故障理由不自动停用设备。
- 文案按用户新要求统一“当前座位”，存储 round/API 名称不变。标题居中、班级独立、连接状态同色加粗。

## 已实现

- 在线选座：64 座、新模板每组四人、IP/会话防重复、重名教师处理、当前座位与历史存档、设备配置及学生私密备注。
- 界面：独立功能导航，最小 16px，标题突出当前上课班级。
- 考勤：每节课堂名单快照、临时换座、并发冲突保护、截止后迟到签到、长期请假延续与签到解除、教师纠正、日期查询和 CSV 日志。
- 随机点名：未发起签到用课堂座位快照，已发起签到只用到场名单，服务端随机和结果记录。
- 班级数据：单个/批量移入回收站和恢复；删除关闭该班登记和课堂，保留历史、设备配置。
- 新模板：原装配置自动升级，当前座位保持姓名并切换 v2 分组，旧存档使用 v1 模板；自定义配置不覆盖。

## 已确定的规则

每节课点击“开始新的一节课”；发起签到是独立操作，不清空座位。课堂名单取开始时当前座位中的学生，不包含未就座学生资料。历史名单不会因改名/换座而变化。结束签到后教师仍可纠正，结束本节课后只读。长期请假是独立考勤标记，不从学生自由备注推断。

新签到迟到从签到截止时间计算，不再单独设置宽限；应到包含请假，实到包含迟到。未发起签到的课堂不自动认定缺勤。同 IP、学生、座位在本节签到中各只允许一条；IP 不是身份验证，仍无法防止冒选其他人的姓名。重名需教师补签。

当前班级切换或座位登记更新后旧课堂不能继续提交；隔天旧课堂不再作为当前课堂，必须开始新课。每次点名独立随机，可能重复。历史考勤保留结束前最终状态，没有完整的每次纠正审计链。

## 尚未实现与限制

- AI 对话采集为下一优先事项：见 AI课堂对话采集方案.md。本次没有 API 调用、密钥保存或对话入口。
- 噪声检测、小游戏、学号导入、不重复公平点名、考勤批量纠正、永久销毁回收站未实现。
- 考勤 CSV 可在 Excel/WPS 中打开，不是原座位模板的额外工作表。
- 随机点名历史已保存，可由接口按课堂读取；独立历史检索界面待补充。
- 新版 Win7/极域实机、新模板在 Excel/WPS 中实际打印、麦克风测试未执行。

## 维护与验证

58 项后端测试通过；原座位浏览器检查、新模块浏览器工作流通过。实际 v1.4.0 EXE 验证通过，打包结果见验收记录。全部使用合成数据与临时目录。

升级到 schema 5 自动备份；schema 2 跳过备份仅属于旧版测试例外。不要修改已发布迁移。先读 AGENTS、CHANGELOG、开发说明、MAINTENANCE，操作方法见 全部功能与流程、功能测试指南，后续方案见 ROADMAP。决定、限制与验证写在项目内，不依赖 AI 账号或聊天。
