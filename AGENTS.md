# ClassroomAssistant 开发交接入口

这是教师机运行的 Windows 局域网课堂助手，不是云网站。开始工作先读：
1. `docs/PROJECT_STATUS.md`：当前版本、完成项、未验证项。
2. `CHANGELOG.md`：各版本变化。
3. `docs/开发维护.md`：架构接口、数据库、升级、构建、验证与数据边界。
4. `docs/ROADMAP.md`：未来功能，只是建议，未经用户要求不要开始实现。

## 已确定的约束

- 1.9.0：schema仍8。Waitress12线程/400连接/10秒空闲/5秒清理；诊断日志不含学生资料和密钥。64Chromium页面611秒通过，不等于64台Win7整节课。学生视角模板经用户2026-09-12确认，所有学生座位图旋转180度，讲台在上，座位号及教师视角/导出映射不变。班级人数为当前座位人数，改名/届数不改历史课堂快照。现场故障未最终定因，见服务长时间加载排查报告。

- 1.8.0：schema8学生职务标记及历史快照，迁移1—8不可改。开始上课拒绝重复，切班通过attendance.publish_class带expected_lesson_id确认并原子下课；开新登记先下课。README架构沿用v1.6.0的Mermaid流程图，不替换为图片。整体UI保持不重做。

- 1.7.0：schema7毕业届数固定整数，不推算旧grade；迁移1—7不可改。班级全CSV由各模块export_data提供，含私密备注仅教师使用。点名候选逐个动画后抽取，通知只在内存5秒、只返回被选学生设备；无OS强制全屏。用户本次旧测试库清除被工具审批拦截，未删除；正式试跑后的数据必须保留。

- 1.6.0：schema 6 已发布，不修改迁移1—6。日志集中班级数据管理；临时/长期均审批；撤销签到授予单生重签直到下课。first_signed_at用于到场迟到，signed_at为最新设备签到；事件保留旧新IP。点名仅保存每班最后手动范围，结果不保存。浏览器演示至少50名虚构学生。

- 1.5.0：README 是唯一使用指南；开发维护是唯一技术说明，旧指南与 AI 独立方案已合并。新点名不创建课堂、不落库；学生公开长期请假状态（不公开备注）；补签在座位弹窗。

- 1.4.0：截止时间仅区分正常/迟到，不自动关闭签到；手动结束才禁止提交。班级新增 year/semester，schema 5，不修改已发布迁移 1—5。完整功能表、流程图和测试指南每次发布同步。

- 1.3.0：学生未开放选座时默认考勤页；点击座位输入姓名签到，禁止轮询重建输入控件。长期换座需教师审批，并通过座位服务修改当前固定座位，不能覆盖他人。签到截止由服务端执行。

- 1.2.0 已实现考勤、随机点名、班级回收站。AI 对话采集仅方案，下一阶段优先；见 docs/ROADMAP.md 的 AI 方案。每节课独立快照，临时换座不修改座位安排；长期请假再次签到解除。
- 当前模板 classroom-64-v2，历史 v1 保留，设备是同一物理机房。原装配置按 JSON 内容等价和 Excel hash 升级，不覆盖自定义配置。

- GitHub 仓库 `chuiyukong/classroom-assistant` 按用户 2026-09-09 的明确要求暂时公开，方便异地访问；正式运营后由用户改为私有。不要自动改回私有。源码和发布包始终不得包含真实学生资料、运行数据库、密钥或日志。

- 教师机 Win10 x64；学生机 Win7 + Chrome/Firefox；学生端无需安装，无外网依赖。
- 模块化单体：Flask + Waitress + SQLite；普通 ES5 前端，避免新浏览器语法；保留原 Excel 模板。
- 用户界面按最新要求称“当前座位 / 历史存档”；存储和 v1 API 保留 `rounds` / `round`，不要为术语重命名做破坏性迁移。
- 同一来源 IP 在同一次登记只允许一次，无 10 分钟过期；新登记、新班级登记重新计数。IP 不是个人身份，不信任转发头。
- 学生端同名拒绝并引导教师；教师可建立独立同名学生记录。姓名不是主键。
- 设备配置跨班级；学生备注绑定 student_id，私密信息不能进入学生接口、页面、常规 Excel。
- 当前安排是后续功能的统一读取入口。各模块不通过 Excel 交换数据、不跨模块直接改表。
- 学生备注存档时保存快照；设备设置始终反映当前物理设备状态。
- 用户明确本次旧库仅为测试数据、不需要备份：仅升级到 schema 2 时跳过备份。之后升级保留备份。不要把此例外泛化为永久不备份。

## 修改流程

- 所有验证使用临时/测试目录，不在默认用户数据目录运行开发服务，不读取/输出学生个人信息。
- 不修改已发布迁移，新增数据库结构使用递增迁移。版本号在 `classroom/version.py`。
- 运行有针对性的 pytest；页面变化运行 `scripts/check_browser.py`；交付 EXE 前构建并运行 `scripts/check_package.py`。
- Win7/极域/麦克风实机测试未执行时必须明确写明，不把模拟 IP 当成真实机房设备。
- 同步更新 PROJECT_STATUS、CHANGELOG、验收记录；架构变化同步开发维护说明；将未实现项留在 ROADMAP。
- `scripts/build.ps1` 生成带版本的包；用户数据不得进入程序包或源码包。
- 不依赖聊天历史、AI 账号或 Codex 私有记忆作为唯一上下文；决定和验证记录写入本项目文档。
