# 版本变更记录

## 1.7.0 · 2026-09-09

- 座位收窄至四汉字，姓名15px常规/状态12px；固定整数列宽与1px留白竖线；停用更浅灰、请假更淡亮黄，统一窄讲台。学生不显示考勤图例，教师合并已签到/迟到并补缺勤。
- 学生成功提示去掉“姓名”前缀，登记/签到均使用加粗浅底姓名标签。
- 点名先随机顺序遍历当前范围，再显示服务端结果；候选变更拒绝陈旧抽取。仅被选学生的已识别电脑显示网页内全屏姓名5秒，无结果持久化。
- 长期审批新增写入校验与回读、成功后当前座位直达链接、窗口重新聚焦刷新；50人测试验证已打开选座页同步更新。
- schema7毕业届数为固定毕业年份，旧年级不推算。增加所选班级全部CSV导出，含模块记录、私密备注与共用设备参考，不含运行密钥。
- 测试继续至少50人；用户本次旧测试数据清理获授权，但工具审批拦截删除，未清空。未来实跑数据保留。


## 1.6.0 · 2026-09-09

- 统一紧凑座位图：数字竖线、常规字重姓名、较小状态文字，最多五字完整显示；学生标题仅班级名、移除重复标题与页面署名，导航按钮及开放状态更醒目。
- 正常和迟到签到同绿，缺勤/关闭/断线统一浅红，未签到蓝灰，请假黄色，停用/原换座灰；点名原换座为空位白色。恢复禁选鼠标提示，登记成功显示学生姓名。
- 上下课操作改为“开始上课 / 下课”，保留课堂起止与班级快照。日志集中到班级数据管理，按年级/学期/班级/日期选课，提供座位图、时间/IP提示、CSV与教学日志预留入口。
- schema 6：班级年级与课堂分类快照、指定重签权限、首次签到时间、设备签到/纠正事件、换座类型、每班最后手动点名范围。旧迁移不改，升级保留自动备份。
- 临时/长期换座均提交审批；临时不改固定座位。修正从原位点击时实际座位显示旧值的问题。撤销为未签到后，即使已结束签到，仅该学生可换机重签；下课全部只读。
- IP来自实际学生请求；教师手动补签不伪造设备IP。换机保留首次到场时间与旧新设备记录。未到场名单按按钮展开。
- 点名增加迟到、手动多选范围；每班只保存最后一次手动选择，结果不落库。恢复README技术架构图，同步流程图与50人测试指南。


## 1.5.0 — 2026-09-08

- 补回 README 的 1.3.0 版本进展，纠正 1.4.0 摘要和验证数字；使用说明统一 README，AI 完整方案并入 ROADMAP，开发与维护合为一份现行手册，删除旧指南和重复文件。
- 平台显示名称统一“智慧课堂综合平台”，保留“课”标识，页面底部标注开发者 Kong；程序和数据目录标识不改名。
- 重建共享 UI 样式：常规 16px 座位姓名、居中显示、六字宽度、适当大组间距、加深讲台；状态与连接同排柔和配色，区分未签/空位/迟到/换座等状态。
- 教师选座按新建、查看与设为当前、登记导出三行组织，年份当前年前后十年；移除独立学生备注入口，保留座位弹窗备注及原数据。
- 学生在考勤页遇教师开放登记自动跳选座；已签到仅显示已签到，未签到在截止后显示签到超时。长期请假状态可在学生座位展示，私密备注仍不公开。
- 教师直接点座位弹窗纠正/补签，长期审批和日志在大屏右侧工具栏，小屏回流；历史仍只读。
- 新点名入口不创建课堂、不落库，无当前考勤用固定座位，有考勤用到场名单；显示候选人数，多人避免紧邻重复，单人明确提示。旧版记录和接口保留。
- 数据库保持 schema 5，无迁移。后端 61 项测试通过；浏览器覆盖一分钟真实时限、自动跳转、弹窗、公开状态与隐私、六字姓名、多候选点名；实际 EXE 和现场未测项见验收记录。

## 1.4.0 — 2026-09-08

- 签到取消前端迟到宽限；截止时刻起仍接受签到并计为迟到，倒计时改为已超时时长。教师手动结束签到/课堂后才拒绝学生提交，未签到转缺勤。旧已关闭课堂不自动重开。
- 教师/学生使用一致的深绿开放、橙色迟到签到、灰色关闭状态块；收紧 64 座图和组间距，点名在 1366×768 下展示完整座位与讲台；未签到座位增加浅红提示。
- “开始新的一节课”改为“开始本节课堂”，明确考勤/点名共用一次课堂快照，不重复开始。
- 新建班级独立选择年份、上/下学期；同名班可按学期区分，数据管理增加筛选。其他课堂页仅显示班级名；旧班保留并标为未设置学期。
- schema 5 重建班级唯一约束为名称+年份+学期，保留编号及全部关联；自动备份，不修改已发布迁移。
- 新增完整功能流程图、全部功能说明、核心与完整人工验收指南；README 收录总览，每次发布同步维护。
- 验证：58 项 pytest、原选座浏览器检查、真实一分钟签到跨截止及手动关闭检查通过；实际 EXE 检查见验收记录。Win7/极域与 Excel/WPS 新模板打印仍待现场复测。

## 1.3.0 — 2026-09-08

- 修复原装模板不升级：旧 Windows JSON 的 CRLF 换行造成 hash 不同，现按 JSON 内容识别原装配置，Excel 仍核验原文件；不覆盖真实自定义布局。
- 学生首页按教师登记开放状态路由；未开放直接进入考勤页，隐藏选座入口，考勤未发起也能查看座位。
- 用稳定座位图替代学生姓名/座位下拉框；点击座位输入姓名，自动更新不重建输入控件，保留焦点和换座原因。
- 签到显示服务端时间校准的倒计时，支持 1—40 分钟（默认 10）；到时拒绝学生签到并将未签到计为缺勤，教师仍可补签。
- 临时换座记录设备故障/长期申请原因；教师批准长期申请后通过座位业务接口更新当前座位，拒绝覆盖固定登记或陈旧申请；历史快照不变。
- 统一居中标题、班级副标题、最小 16px 字号和连接状态颜色；教师改称“当前座位”，只在存档显示日期。
- 随机点名移除签到操作，按教室布局播放高亮动画，结果只显示姓名并框选座位。
- schema 4 新增签到截止时间、课堂布局、换座原因和申请记录；保留 schema 1—3 迁移，升级前自动备份。
- 验证：55 项 pytest、选座及模块浏览器流程通过。Win7/极域实机与新模板实际打印仍需复测；AI 功能未开发。

## 1.2.0 — 2026-09-07

- 移除宣传式页面文案，明确“在线选座系统”；统一最小 16px 字号，突出当前上课班级，新增考勤、点名、数据管理导航。
- 采用用户新 Excel 模板，每小组四座。当前安排保持姓名升级布局，历史存档保持旧版布局和导出；设备状态按物理座位继续共享。自定义配置不自动覆盖。
- 新增独立课堂、考勤快照和日志：临时换座、每机/每生/每座防重复、教师补签、迟到时长、请假/缺勤/长期请假、实时统计、按班级日期查询、CSV 导出。
- 长期请假在后续课堂延续，再次签到后解除；旧日志不回写。结束课堂后只读。
- 新增随机点名与动画：未发起签到使用座位快照，已发起签到仅抽取已签到人员，记录结果。
- 班级支持单个/批量回收站删除与恢复，保留历史和设备配置。
- schema 3 新增课堂、考勤、长期请假和点名表及班级回收站字段；升级前恢复自动备份。
- AI 对话采集加入优先方案，尚未实现；噪声/小游戏未开工。
- 验证：48 项 pytest、原选座浏览器流程、新模块浏览器流程通过；机房旧版连接正常由用户反馈，新版实机与打印待复测。

## 1.1.0 — 2026-09-06

来自用户首轮机房测试反馈的改进：

- 修复切换浏览器可重复登记：增加服务端来源 IP 限制与数据库唯一索引，保留会话限制。限制只作用于本次登记，不采用 10 分钟冷却。
- 学生提交同名时拒绝，提示到教师端登记；规范化空白、全半角与大小写后比较。教师可显式建立独立同名学生记录。
- 界面改为“当前安排 / 历史存档”。发起新登记时旧安排自动存档并关闭；并发或旧页面重复发起受当前安排版本校验保护。
- 增加“设为当前上课班级”，持久化学生页面所显示的班级；重启服务仍显示该班当前安排。仅查看班级/存档不会改变学生页面。
- 增加跨班级设备停用状态及设备备注。停用时保留已有登记，但拒绝学生选择，恢复后可重新使用。
- 增加学生独立编号和教师私密备注，支持未就座学生记录；清除座位不删除学生备注，重新登记唯一同名记录可恢复关联。同名歧义由教师选定。
- 存档冻结学生备注快照；设备状态仍显示当前状态。学生接口不返回私密备注、设备问题详情或来源 IP。
- 保持原 Excel 模板，仅导出姓名、班级和人数；导出文件名使用当前安排/存档。
- 数据库 schema 1 → 2 兼容迁移，保留测试记录。按用户要求，此次目标 schema 2 的测试库升级跳过备份，未来升级继续自动备份。
- 增加跨账号/AI 交接入口、开发状态、维护手册、路线图和本变更记录。噪声、考勤、点名和游戏尚未开发。
- 验证：39 项 pytest、Chromium 交互、64 HTTP 客户端模拟独立 IP 并发及实际 EXE 检查通过；Win7/极域现场联机仍待复测。

## 1.0.0 — 2026-09-06

- 首次实现教师机便携程序、局域网学生登记、64 座模板布局、班级与登记历史、教师纠错、原模板 Excel 导出。
- 使用 Flask、Waitress、SQLite，模块化单体；源代码、打包脚本和测试随项目交付。
- 27 项后端测试及浏览器、64 客户端并发、Excel 实际打开/打印、独立 EXE 检查通过。
- 已知不足：仅浏览器会话限制，换浏览器可再次登记；缺少设备配置和学生备注；界面使用“轮次”。这些问题在 1.1.0 处理。

## 后续记录格式

每次发布记录日期、版本、用户问题、行为变化、数据库变化、验证结果和剩余限制。未完成内容不写成已修复。计划与实现分开维护；历史已发布记录不静默改写。
